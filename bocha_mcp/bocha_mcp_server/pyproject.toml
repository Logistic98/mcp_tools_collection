[project]
name = "bocha_search_mcp"
version = "0.0.1"
description = "A"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
 "fastmcp>=0.4.0",
 "httpx>=0.27.0",
 "mcp[cli]>=1.6.0",
 "python-dotenv>=1.0.1",
]

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
bocha-search-mcp = "bocha_search_mcp:main"
